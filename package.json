{
    "name": "Level Generator",
    "version": "1.0.0",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "start": "cd client && npm run build && cd .. && uvicorn main:app --reload"
    }
  }
  